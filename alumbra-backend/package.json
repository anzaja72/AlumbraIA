
{
  "name": "alumbra-backend",
  "version": "1.0.0",
  "description": "Backend service for Alumbra AI application.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint .",
    "format": "prettier --write \"**/*.js\""
  },
  "keywords": [
    "alumbra",
    "ai",
    "backend",
    "express"
  ],
  "author": "Alumbra AI Team",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "helmet": "^7.0.0",  
    "morgan": "^1.10.0"
    // Add other dependencies like:
    // "mongoose": "^7.0.0", (for MongoDB)
    // "jsonwebtoken": "^9.0.0", (for auth)
    // "bcryptjs": "^2.4.3", (for password hashing)
    // "joi": "^17.9.1", (for validation)
    // "@genkit-ai/googleai": "latest", (if using Genkit here)
    // "genkit": "latest"
  },
  "devDependencies": {
    "eslint": "^8.30.0",
    "nodemon": "^2.0.20",
    "prettier": "^2.8.1"
  }
}
